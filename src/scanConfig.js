// Base URL of the AI document-scan backend (Render Web Service).
// Only used for photo/PDF extraction. Excel/CSV import works without it.
export const SCAN_BACKEND_URL = 'https://suba-stock-scan-backend.onrender.com';
